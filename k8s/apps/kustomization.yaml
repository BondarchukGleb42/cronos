apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cronos-bot
resources:
  - 10-artifacts.yaml
  - 20-gateway.yaml
  - 30-runtime.yaml
images:
  - name: cronos-app
    newName: cr.yandex/crpe8jmbmklfe4l31c68/cronos
    newTag: IMAGE_TAG
# migration-job.yaml is rendered and run separately before these workloads.
