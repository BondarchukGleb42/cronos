# Отдельные чаты и названия тем

Отдельные разговоры используют настоящие темы личного чата Telegram: создание доступно из интерфейса Telegram, кнопкой бота, командой и обычной просьбой к агенту.

## Пользовательский сценарий

- `/new` и кнопка «➕ Новый чат» создают настоящую тему в личном чате с ботом. После создания бот отправляет приветствие в новую тему. Попросить создать тему можно и обычным текстом через инструмент `topic_create`.
- `/chats` показывает до 30 сохранённых названий и кнопку создания чата. Все доступные темы и переключение между ними находятся в штатном списке тем Telegram.
- Пользователь также может создавать темы средствами самого Telegram, если владелец бота включил соответствующее разрешение. Служебные события создания и переименования синхронизируются с Cronos.
- История разговора разделена по темам; сохранённая память и предпочтения общие для пользователя. Новый чат не стирает память и не подмешивает историю другой темы.
- Явное название, заданное пользователем при создании темы в Telegram или при ручном переименовании, закрепляется: автоматическое переименование отключается для этой темы.
- «🗑 Удалить этот чат», `/delete` и просьба удалить чат запускают подтверждение удаления темы, её локальной истории и расписаний. Полная очистка всех данных — `/clearall`. Подробности — в [privacy.md](privacy.md).

Cronos не обещает автоматически переключить открытый экран Telegram. Документированного jump URL для конкретной темы **личного чата с ботом** нет, поэтому такие ссылки не строятся. Обычная ссылка открывает самого бота. Официальное описание [forum topic links](https://core.telegram.org/api/links#forum-topic-links) относится к ссылкам на сообщения и темы групп/каналов.

## Автоматическое название

Первое название ставится после сохранения первого сообщения пользователя и ответа ассистента. Следующие обновления запрашиваются на отметках **10, 20, 30… сообщений суммарно**, а не через каждые десять пар. Считаются только сохранённые сообщения `user` и `assistant`, не исключённые из истории. Служебные события, вызовы инструментов и генерация самого названия в этот счётчик не входят. Для обычного диалога без native topic автоматическое название не создаётся.

`MODEL_TITLE` выбирает отдельную модель; значение по умолчанию — `meta-llama/llama-3.1-8b-instruct`. [Публичный каталог AllTokens](https://alltokens.ru/models/meta-llama/llama-3.1-8b-instruct) подтверждает доступность этой небольшой текстовой модели. Разговорная модель и её резервные маршруты при создании названия не используются.

В запрос передаются максимум четыре последних текстовых сообщения по 800 символов и прежнее название. История рассматривается как данные, инструменты и поиск не подключаются. Лимит ответа — 64 токена; таймаут одного запроса — 10 секунд. Промпт предпочитает название из 1–2 слов, допускает больше только для сохранения смысла и просит обозначить основную тему без лишних дат, мест и уточнений. Короткая фраза сохраняется целиком. Название длиннее 64 символов или 128 байт UTF-8 отклоняется с сохранением данных о стоимости вместо обрезки слов.

## Выполнение и восстановление

Переименование — отдельное сохраняемое событие `topic_title` в PostgreSQL, которое обрабатывает существующий Worker. Основной ответ не ждёт генерации названия. Новые процессы, базы и очереди не добавляются.

Событие различается по пользователю, теме, ревизии и порогу счётчика. Перед работой проверяются актуальность ревизии и разрешение автоматического названия. Ручное переименование меняет ревизию и отключает автоматику, поэтому старое задание не должно заменить выбор пользователя.

Порядок ручных переименований определяется `message_id` служебного сообщения Telegram, сохранённым в `title_update_id`. Старое событие игнорируется; повтор последнего события позволяет снова подтвердить актуальное имя в Telegram. Повтор события создания только находит существующую тему и не возвращает её прежнее название.

Результат модели сохраняется до вызова Telegram `editForumTopic`. Если Telegram не подтвердил переименование, повтор обработки использует уже сохранённый результат модели. Стоимость проходит через существующий механизм резервирования и учёта usage. Пустой или слишком длинный ответ модели не считается успешным названием.

Создание темы также сохраняет намерение операции: при неопределённом результате Telegram повтор не создаёт ещё одну тему вслепую. Повторные вызовы `topic_create` с тем же нормализованным названием внутри одного запуска агента используют одну операцию, даже если модель выдала новый ID вызова инструмента. Новый самостоятельный запрос пользователя может создать ещё одну тему с таким же названием.

## Включение и границы проверки

Последний runtime-снимок `getMe` для `@cronos_ait_bot` от **07.09.2026**:

| Поле | Значение | Состояние |
| --- | --- | --- |
| `has_topics_enabled` | `true` | Пользователь включил Topics Mode; бот может создавать темы |
| `allows_users_to_create_topics` | `true` | Самостоятельное создание и удаление тем пользователем через native UI разрешено |

Это независимые возможности: первая необходима для работы тем, вторая — для самостоятельного создания пользователем через Telegram. `/new` и кнопка вызывают создание от имени бота и проверяют первую. Значения описаны в официальном объекте [User, возвращаемом getMe](https://core.telegram.org/bots/api#user). Успешный ответ `getMe` кешируется на 60 секунд, после чего изменения BotFather могут быть обнаружены без перезапуска.

В BotFather для этого выключен запрет «Disallow users to create new threads». Создание и ручное переименование приходят как служебные события и синхронизируются с Cronos. Неявное имя (`is_name_implicit`) не закрепляет название: его продолжает обновлять модель.

Удаление через интерфейс Telegram не имеет отдельного надёжного события в Bot API. Поэтому удаление темы в клиенте само по себе не гарантирует очистку её данных в Cronos: для этого нужно использовать `/delete` или кнопку бота. [Темы ботов](https://core.telegram.org/api/forum#bot-forums), [события Bot API](https://core.telegram.org/bots/api#update).

## Историческая проверка тем: 6 сентября 2026

При проверке runtime `8722f04b18901d03ff40ba05fcdd9d0e8aa2e3b7` все этапы [CI 34053942382](https://github.com/BondarchukGleb42/cronos/actions/runs/34053942382) завершились успешно: **219 pytest-тестов и 9 проверок kubeconfig**, сборка и деплой. Все пять постоянных pod были готовы, перезапусков на снимке не было; gateway `/readyz` возвращал `ready`. Это снимок указанной версии, а не состояние последней выкладки.

| Проверка | Подтверждённый результат |
| --- | --- |
| Создание native topic | Telegram создал настоящую тему личного чата; операция сохранена |
| Доставка в тему | Приветствие и форматированный ответ с кнопками отправлены; Telegram вернул идентификаторы сообщений |
| Реальный разговор | Пользователь уже обменялся сообщениями с агентом в созданной теме |
| Первое автоназвание | Фоновое задание завершилось со статусом `done` после двух сохранённых сообщений — пользователя и ассистента |
| Модель и учёт | Использована `meta-llama/llama-3.1-8b-instruct`; usage сохранён, стоимость названия — **0,001430 ₽** |
| Восстановление создания | Повторно использована та же операция создания без дубликата; в БД осталась одна строка этой темы |
| Меню команд | `/new`, `/chats`, `/stop`, `/plans`, `/start` подтверждены через Bot API |

Сохранение операции создания без `run_id` исправлено и покрыто отдельным интеграционным тестом настоящего PostgreSQL. Тексты пользовательских вопросов и названий, а также личные идентификаторы в отчёт не включаются.

Пороги **10/20 сообщений**, ручное закрепление названия, порядок повторных служебных событий и разделение истории покрыты тестами. Для проверки этих порогов дополнительная переписка от имени пользователя не создавалась. **Визуальные клики и переключение тем в клиенте Telegram не наблюдались**; подтверждение через API, доставку и хранилище не выдаётся за визуальную проверку. Разрешение создания темы через native UI впоследствии включено и проверено 7 сентября, как указано выше.

Основные проверки реализации:

```sh
uv run pytest -q tests/test_topics.py tests/test_topic_title_provider.py tests/test_storage.py tests/test_telegram.py
```

Для интеграционных проверок `test_storage.py` нужен отдельный тестовый PostgreSQL, как описано в [README](../README.md#разработка).
